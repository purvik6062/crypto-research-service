services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    restart: always

  asksurf-service:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./session:/app/session
    depends_on:
      - redis
    restart: always
